<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ offer.titre }} | TalentHub</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-slate-50 text-slate-600 antialiased min-h-screen flex flex-col">

    <nav class="bg-white border-b border-slate-200 sticky top-0 z-30">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center gap-3">
                    <a href="/jobs" class="flex items-center gap-2">
                        <div class="w-8 h-8 bg-indigo-600 rounded-lg flex items-center justify-center text-white font-bold">T</div>
                        <span class="text-xl font-bold text-slate-800">TalentHub</span>
                    </a>
                </div>
                <div class="flex items-center gap-4">
                    {% if session.user %}
                        <a href="/candidate/dashboard" class="px-4 py-2 text-sm font-medium text-indigo-600 bg-indigo-50 hover:bg-indigo-100 rounded-lg transition-colors">
                            Dashboard
                        </a>
                    {% else %}
                        <a href="/login" class="text-sm font-medium text-slate-500 hover:text-indigo-600">Login</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </nav>

    <header class="bg-white border-b border-slate-200 py-12">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="max-w-4xl">
                <a href="/jobs" class="inline-flex items-center gap-1 text-sm text-slate-400 hover:text-indigo-600 mb-6 transition-colors">
                    &larr; Back to Jobs
                </a>

                <div class="flex items-start justify-between gap-6 flex-wrap">
                    <div>
                        {% if offer.category_name %}
                            <span class="inline-block px-3 py-1 rounded-full text-xs font-semibold bg-indigo-50 text-indigo-600 border border-indigo-100 mb-4">
                                {{ offer.category_name }}
                            </span>
                        {% endif %}

                        <h1 class="text-3xl md:text-4xl font-bold text-slate-900 mb-4">{{ offer.titre }}</h1>
                        
                        <div class="flex flex-wrap items-center gap-6 text-slate-500 font-medium">
                            <div class="flex items-center gap-2">
                                <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path></svg>
                                {{ offer.nom_entreprise }}
                            </div>
                            <div class="flex items-center gap-2">
                                <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path></svg>
                                {{ offer.lieu }}
                            </div>
                            <div class="flex items-center gap-2 text-slate-700">
                                <svg class="w-5 h-5 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                                ${{ offer.salaire|number_format }} <span class="text-slate-400 font-normal">/ year</span>
                            </div>
                        </div>
                    </div>

                    <div class="text-sm text-slate-400 text-right">
                        Posted {{ offer.created_at|date("M d, Y") }}
                    </div>
                </div>
            </div>
        </div>
    </header>

    <main class="flex-1 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10 w-full">
    <div class="max-w-7xl mx-auto px-4 p-6 mt-6">
        {# Check 'flash.success' instead of 'session.success' #}
        {% if flash.success %}
            <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
                <strong class="font-bold">Success!</strong>
                <span class="block sm:inline">{{ flash.success }}</span>
            </div>
        {% endif %}

        {% if flash.error %}
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
                <strong class="font-bold">Error!</strong>
                <span class="block sm:inline">{{ flash.error }}</span>
            </div>
        {% endif %}
    </div>
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-10">
            
            <div class="lg:col-span-2 space-y-8">
                <section class="bg-white rounded-2xl p-8 border border-slate-200 shadow-sm">
                    <h2 class="text-xl font-bold text-slate-900 mb-6">Job Description</h2>
                    <div class="prose prose-slate max-w-none text-slate-600 leading-relaxed">
                        {{ offer.description|nl2br }}
                    </div>
                </section>

                {% if tags is not empty %}
                <section class="bg-white rounded-2xl p-8 border border-slate-200 shadow-sm">
                    <h2 class="text-lg font-bold text-slate-900 mb-4">Skills Required</h2>
                    <div class="flex flex-wrap gap-2">
                        {% for tag in tags %}
                            <span class="px-3 py-1.5 rounded-lg text-sm font-medium bg-slate-100 text-slate-700 border border-slate-200">
                                {{ tag.nom }}
                            </span>
                        {% endfor %}
                    </div>
                </section>
                {% endif %}
            </div>

            <div class="space-y-6">
                
                <div id="apply" class="bg-white rounded-2xl p-6 border border-slate-200 shadow-lg shadow-indigo-100 sticky top-24">
                    <h3 class="font-bold text-xl text-slate-900 mb-2">Apply for this job</h3>
                    <p class="text-sm text-slate-500 mb-6">Read the requirements carefully before applying.</p>
                    
                    {% if session.user %}
                        <form action="/candidate/applications/apply/{{ offer.id }}" method="POST">
                            <button type="submit" class="w-full py-4 bg-indigo-600 hover:bg-indigo-700 text-white font-bold text-lg rounded-xl shadow-md shadow-indigo-200 transition-all transform active:scale-95">
                                Apply Now
                            </button>
                        </form>
                        <p class="text-xs text-center text-slate-400 mt-3">
                            You are applying as <span class="text-slate-600 font-medium">{{ session.user.prenom }}</span>
                        </p>
                    {% else %}
                        <a href="/login" class="block w-full text-center py-4 bg-slate-900 hover:bg-slate-800 text-white font-bold rounded-xl transition-colors">
                            Login to Apply
                        </a>
                        <p class="text-xs text-center text-slate-400 mt-3">
                            Don't have an account? <a href="/register" class="text-indigo-600 hover:underline">Register</a>
                        </p>
                    {% endif %}
                </div>

                <div class="bg-slate-50 rounded-2xl p-6 border border-slate-200">
                    <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider mb-4">About the Company</h3>
                    <div class="flex items-center gap-3 mb-4">
                        <div class="w-10 h-10 bg-white border border-slate-200 rounded-lg flex items-center justify-center font-bold text-slate-400">
                            {{ offer.nom_entreprise|slice(0,1)|upper }}
                        </div>
                        <div class="font-bold text-slate-900">{{ offer.nom_entreprise }}</div>
                    </div>
                    
                    {% if offer.site_web %}
                        <a href="{{ offer.site_web }}" target="_blank" class="flex items-center gap-2 text-sm text-indigo-600 hover:underline font-medium">
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path></svg>
                            Visit Website
                        </a>
                    {% endif %}
                </div>

            </div>
        </div>
    </main>

    <footer class="bg-white border-t border-slate-200 py-10 mt-auto">
        <div class="max-w-7xl mx-auto px-4 text-center">
            <p class="text-slate-400 text-sm">&copy; {{ "now"|date("Y") }} TalentHub. All rights reserved.</p>
        </div>
    </footer>

</body>
</html>