<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<title>My Profile | TalentHub</title>
		 <script src="https://cdn.tailwindcss.com"></script>
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
		<style>
			body {
				font-family: 'Inter', sans-serif;
			}
		</style>
	</head>
	<body class="bg-slate-50 text-slate-600 antialiased h-screen flex overflow-hidden">

		{% include 'candidate/sidebar.twig' %}

		<main class="flex-1 flex flex-col h-screen overflow-hidden bg-slate-50">

			<header class="h-20 bg-white border-b border-slate-100 flex items-center justify-between px-8">
				<h1 class="text-xl font-bold text-slate-800">My Profile</h1>
				<div class="flex items-center gap-4">
					<a href="/candidate/profile/edit" class="bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-lg text-sm font-medium transition flex items-center gap-2">
						<svg class="w-4 h-4" fill="none" stroke="currentColor" viewbox="0 0 24 24">
							<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"></path>
						</svg>
						Edit Profile
					</a>
				</div>
			</header>

			<div class="flex-1 overflow-x-hidden overflow-y-auto p-8">

				{% if session.success %}
					<div class="mb-6 bg-green-50 text-green-700 px-4 py-3 rounded-lg border border-green-200">
						{{ session.success }}
					</div>
				{% endif %}

				<div class="max-w-4xl mx-auto space-y-6">

					<div class="bg-white rounded-2xl shadow-sm border border-slate-200 overflow-hidden">
						<div class="h-32 bg-gradient-to-r from-indigo-500 to-purple-600"></div>
						<div class="px-8 pb-8 flex flex-col md:flex-row items-end -mt-12 gap-6">
							<div class="w-32 h-32 rounded-2xl bg-white p-1 shadow-lg">
								<div class="w-full h-full bg-slate-100 rounded-xl flex items-center justify-center text-4xl font-bold text-slate-400">
									{{ profile.nom|slice(0,1)|upper }}
								</div>
							</div>
							<div class="flex-1 pb-2">
								<h2 class="text-2xl font-bold text-slate-900">{{ profile.nom }}
									{{ profile.prenom }}</h2>
								<p class="text-indigo-600 font-medium">
									{% if profile.experience_annee %}
										{{ profile.experience_annee }}
										years of experience
									{% else %}
										Candidate
									{% endif %}
								</p>
								<div class="flex flex-wrap gap-4 mt-3 text-sm text-slate-500">
									<div class="flex items-center gap-1">
										<svg class="w-4 h-4" fill="none" stroke="currentColor" viewbox="0 0 24 24">
											<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
										</svg>
										{{ profile.email }}
									</div>
									{% if profile.telephone %}
										<div class="flex items-center gap-1">
											<svg class="w-4 h-4" fill="none" stroke="currentColor" viewbox="0 0 24 24">
												<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
											</svg>
											{{ profile.telephone }}
										</div>
									{% endif %}
								</div>
							</div>
						</div>
					</div>

					<div class="grid grid-cols-1 md:grid-cols-3 gap-6">
						<div class="space-y-6">
							<div class="bg-white rounded-2xl shadow-sm border border-slate-200 p-6">
								<h3 class="font-bold text-slate-800 mb-4">Availability</h3>
								<div class="flex items-center gap-2 text-sm">
									<span class="w-2 h-2 rounded-full bg-green-500"></span>
									Not specified
								</div>
								<h3 class="font-bold text-slate-800 mt-6 mb-4">Salary Expectations</h3>
								<div class="text-sm font-medium text-slate-600">
									{% if profile.expected_salary %}
										${{ profile.expected_salary|number_format }}
										/ year
									{% else %}
										Not specified
									{% endif %}
								</div>
							</div>

							<div class="bg-white rounded-2xl shadow-sm border border-slate-200 p-6">
								<h3 class="font-bold text-slate-800 mb-4">Skills</h3>
								<div class="flex flex-wrap gap-2">
									{% if profile.skills %}
										{% set skillsList = profile.skills|split(',') %}
										{% for skill in skillsList %}
											<span class="px-3 py-1 bg-indigo-50 text-indigo-700 rounded-full text-xs font-medium border border-indigo-100">
												{{ skill|trim }}
											</span>
										{% endfor %}
									{% else %}
										<span class="text-slate-400 text-sm italic">No skills added yet.</span>
									{% endif %}
								</div>
							</div>
						</div>

						<div class="md:col-span-2">
							<div class="bg-white rounded-2xl shadow-sm border border-slate-200 p-8">
								<h3 class="font-bold text-slate-800 mb-6 text-lg">About & Experience</h3>
								<div class="grid grid-cols-1 md:grid-cols-2 gap-6">
									<div class="p-5 rounded-2xl bg-slate-50 border border-slate-100">
										<span class="text-xs font-bold text-slate-400 uppercase tracking-widest">Experience</span>
										<p class="text-2xl font-bold text-slate-800 mt-1">{{ profile.experience_annee }}
											Years</p>
									</div>
									<div class="p-5 rounded-2xl bg-slate-50 border border-slate-100">
										<span class="text-xs font-bold text-slate-400 uppercase tracking-widest">Expected Salary</span>
										<p class="text-2xl font-bold text-slate-800 mt-1">${{ profile.expected_salaire|number_format(0) }}</p>
									</div>
								</div>
							</div>
						</div>
					</div>

				</div>
			</div>
		</main>
	</body>
</html>
