<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>My Profile | TalentHub</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        /* Custom Checkbox Styling for Skills */
        .skill-checkbox:checked + div {
            background-color: #eff6ff; /* blue-50 */
            border-color: #6366f1; /* indigo-500 */
            color: #4f46e5; /* indigo-600 */
        }
        .skill-checkbox:checked + div svg {
            display: block;
        }
    </style>
</head>
<body class="bg-slate-50 text-slate-600 antialiased h-screen flex overflow-hidden">

    {% include 'candidate/sidebar.twig' %}

    <main class="flex-1 flex flex-col h-screen overflow-hidden bg-slate-50 relative">
        
        <header class="h-20 bg-white/80 backdrop-blur-md border-b border-slate-200 flex items-center justify-between px-8 sticky top-0 z-20">
            <div>
                <h1 class="text-xl font-bold text-slate-800">My Profile</h1>
                <p class="text-xs text-slate-500 mt-1">Manage your CV and personal details</p>
            </div>
            
            <div class="flex items-center gap-4">
                 <div class="text-right hidden md:block">
                    <div class="text-sm font-bold text-slate-800">{{ session.user.name }}</div>
                    <div class="text-xs text-slate-500">Candidate</div>
                </div>
                <div class="h-10 w-10 rounded-full bg-gradient-to-br from-indigo-500 to-purple-600 shadow-md flex items-center justify-center text-white font-bold border-2 border-white">
                    {{ session.user.name|slice(0,1)|upper }}
                </div>
            </div>
        </header>

        <div class="flex-1 overflow-x-hidden overflow-y-auto p-4 md:p-8">
            
            {% if session.success %}
                <div class="max-w-5xl mx-auto mb-6 bg-green-50 text-green-700 px-4 py-3 rounded-xl border border-green-200 flex items-center gap-3 shadow-sm">
                    <div class="p-1 bg-green-100 rounded-full">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                    </div>
                    <span class="font-medium">{{ session.success }}</span>
                </div>
            {% endif %}

            <form action="/candidate/profile/update" method="POST" class="max-w-5xl mx-auto grid grid-cols-1 lg:grid-cols-3 gap-8 pb-10">
                
                <div class="lg:col-span-1 space-y-6">
                    
                    <div class="bg-white rounded-2xl shadow-sm border border-slate-200 overflow-hidden">
                        <div class="h-24 bg-gradient-to-r from-indigo-500 to-purple-500"></div>
                        <div class="px-6 pb-6 relative">
                            <div class="absolute -top-12 left-6">
                                <div class="h-24 w-24 rounded-2xl bg-white p-1 shadow-lg">
                                    <div class="h-full w-full rounded-xl bg-slate-100 flex items-center justify-center text-3xl font-bold text-slate-400">
                                        {{ session.user.name|slice(0,1)|upper }}
                                    </div>
                                </div>
                            </div>
                            
                            <div class="mt-14">
                                <h2 class="text-lg font-bold text-slate-800">{{ profile.nom }} {{ profile.prenom }}</h2>
                                <p class="text-sm text-slate-500">{{ profile.titre|default('No Title Set') }}</p>
                            </div>

                            <div class="mt-6 space-y-4">
                                <div>
                                    <label class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-1">First Name</label>
                                    <input type="text" name="prenom" value="{{ profile.prenom }}" required 
                                           class="w-full text-sm font-medium border-b border-slate-200 focus:border-indigo-500 focus:outline-none py-1 transition-colors">
                                </div>
                                <div>
                                    <label class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-1">Last Name</label>
                                    <input type="text" name="nom" value="{{ profile.nom }}" required 
                                           class="w-full text-sm font-medium border-b border-slate-200 focus:border-indigo-500 focus:outline-none py-1 transition-colors">
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="bg-white rounded-2xl shadow-sm border border-slate-200 p-6">
                        <h3 class="font-bold text-slate-800 mb-4 flex items-center gap-2">
                            <svg class="w-5 h-5 text-indigo-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path></svg>
                            Contact & Info
                        </h3>
                        <div class="space-y-4">
                            <div>
                                <label class="block text-xs text-slate-500 mb-1">Email Address</label>
                                <input type="email" value="{{ profile.email }}" disabled 
                                       class="w-full bg-slate-50 border border-slate-200 rounded-lg px-3 py-2 text-sm text-slate-500 cursor-not-allowed">
                            </div>
                            <div>
                                <label class="block text-xs text-slate-500 mb-1">Phone Number</label>
                                <input type="tel" name="telephone" value="{{ profile.telephone }}" placeholder="+1 234..."
                                       class="w-full bg-white border border-slate-200 rounded-lg px-3 py-2 text-sm focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 outline-none transition-all">
                            </div>
                             <div>
                                <label class="block text-xs text-slate-500 mb-1">Location</label>
                                <input type="text" name="adresse" value="{{ profile.adresse }}" placeholder="e.g. New York, USA"
                                       class="w-full bg-white border border-slate-200 rounded-lg px-3 py-2 text-sm focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 outline-none transition-all">
                            </div>
                        </div>
                    </div>

                     <div class="bg-white rounded-2xl shadow-sm border border-slate-200 p-6">
                        <h3 class="font-bold text-slate-800 mb-4 flex items-center gap-2">
                            <svg class="w-5 h-5 text-indigo-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                            Status
                        </h3>
                         <div class="space-y-4">
                            <div>
                                <label class="block text-xs text-slate-500 mb-1">Availability</label>
                                <select name="disponibilite" class="w-full bg-white border border-slate-200 rounded-lg px-3 py-2 text-sm focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 outline-none">
                                    <option value="">Select status...</option>
                                    <option value="Immediate" {% if profile.disponibilite == 'Immediate' %}selected{% endif %}>🟢 Immediate</option>
                                    <option value="1 Month" {% if profile.disponibilite == '1 Month' %}selected{% endif %}>🟡 1 Month Notice</option>
                                    <option value="3 Months" {% if profile.disponibilite == '3 Months' %}selected{% endif %}>🔴 3 Months Notice</option>
                                </select>
                            </div>
                            <div>
                                <label class="block text-xs text-slate-500 mb-1">Min. Salary Expectations</label>
                                <div class="relative">
                                    <span class="absolute left-3 top-2 text-slate-400 text-sm">$</span>
                                    <input type="number" name="salaire_min" value="{{ profile.salaire_min }}" placeholder="0.00"
                                           class="w-full bg-white border border-slate-200 rounded-lg pl-6 pr-3 py-2 text-sm focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 outline-none">
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="lg:col-span-2 space-y-6">
                    
                    <div class="bg-white rounded-2xl shadow-sm border border-slate-200 p-8">
                        <div class="flex items-center justify-between mb-6">
                            <h2 class="text-lg font-bold text-slate-800 flex items-center gap-2">
                                <svg class="w-5 h-5 text-indigo-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path></svg>
                                Professional Profile
                            </h2>
                        </div>

                        <div class="space-y-6">
                            <div>
                                <label class="block text-sm font-medium text-slate-700 mb-2">Professional Headline</label>
                                <input type="text" name="titre" value="{{ profile.titre }}" placeholder="e.g. Senior Full Stack Developer | PHP & React Specialist"
                                       class="w-full bg-slate-50 border border-slate-200 rounded-xl px-4 py-3 text-sm focus:bg-white focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 outline-none transition-all placeholder:text-slate-400">
                            </div>

                            <div>
                                <label class="block text-sm font-medium text-slate-700 mb-2">About / Experience</label>
                                <textarea name="experience" rows="6" placeholder="Tell recruiters about your background, key achievements, and what you are looking for..."
                                          class="w-full bg-slate-50 border border-slate-200 rounded-xl px-4 py-3 text-sm focus:bg-white focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 outline-none transition-all placeholder:text-slate-400 resize-none">{{ profile.experience }}</textarea>
                                <p class="text-xs text-right text-slate-400 mt-2">Markdown not supported.</p>
                            </div>
                        </div>
                    </div>

                    <div class="bg-white rounded-2xl shadow-sm border border-slate-200 p-8">
                        <h2 class="text-lg font-bold text-slate-800 mb-2 flex items-center gap-2">
                            <svg class="w-5 h-5 text-indigo-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"></path></svg>
                            Skills & Expertise
                        </h2>
                        <p class="text-sm text-slate-500 mb-6">Select the technologies and skills that best represent your abilities.</p>

                        <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-3">
                            {% for tag in allTags %}
                                <label class="cursor-pointer group relative">
                                    <input type="checkbox" name="tags[]" value="{{ tag.id }}" class="skill-checkbox sr-only"
                                           {% if tag.id in selectedTagIds %}checked{% endif %}>
                                    <div class="border border-slate-200 rounded-xl px-3 py-2 text-sm text-slate-600 bg-white hover:border-indigo-300 hover:shadow-sm transition-all flex items-center justify-between group-hover:text-indigo-600">
                                        <span>{{ tag.nom }}</span>
                                        <svg class="w-4 h-4 text-indigo-500 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                                    </div>
                                </label>
                            {% endfor %}
                        </div>
                    </div>

                    <div class="flex items-center justify-end pt-4">
                        <button type="submit" class="bg-indigo-600 text-white px-8 py-3 rounded-xl font-bold hover:bg-indigo-700 transition-all shadow-lg shadow-indigo-500/20 active:transform active:scale-95 flex items-center gap-2">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7H5a2 2 0 00-2 2v9a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-3m-1 4l-3 3m0 0l-3-3m3 3V4"></path></svg>
                            Save Profile Changes
                        </button>
                    </div>

                </div>

            </form>
        </div>
    </main>
</body>
</html>