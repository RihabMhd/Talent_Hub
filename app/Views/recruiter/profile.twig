<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<title>Company Profile | TalentHub</title>
		 <script src="https://cdn.tailwindcss.com"></script>
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
		<style>
			body {
				font-family: 'Inter', sans-serif;
			}
		</style>
	</head>
	<body class="bg-slate-50 text-slate-600 antialiased h-screen flex overflow-hidden">

		{% include 'recruiter/sidebar.html.twig' %}

		<main class="flex-1 flex flex-col h-screen overflow-hidden bg-slate-50">
			<header class="h-20 bg-white border-b border-slate-100 flex items-center justify-between px-8">
				<h1 class="text-xl font-bold text-slate-800">Your Profile</h1>
				<div class="h-10 w-10 rounded-full bg-indigo-100 border-2 border-white shadow-sm flex items-center justify-center text-indigo-700 font-bold">
					{{ current_user.prenom|slice(0,1)|upper }}
				</div>
			</header>

			<div class="flex-1 overflow-x-hidden overflow-y-auto p-8">
				{% if session.success %}
					<div class="mb-6 bg-green-50 text-green-700 px-4 py-3 rounded-lg border border-green-200 max-w-4xl mx-auto">
						{{ session.success }}
					</div>
				{% endif %}

				<div class="max-w-4xl mx-auto">
					<form action="/recruiter/company" method="POST" class="bg-white rounded-2xl shadow-sm border border-slate-200 overflow-hidden">
						<div class="h-32 bg-gradient-to-r from-blue-600 to-indigo-700"></div>

						<div class="px-8 pb-8 flex flex-col items-start -mt-12 gap-6">
							<div class="w-32 h-32 rounded-2xl bg-white p-1 shadow-lg">
								<div class="w-full h-full bg-slate-100 rounded-xl flex items-center justify-center text-4xl font-bold text-slate-400">
									{{ company.nom_entreprise|slice(0,1)|upper|default('C') }}
								</div>
							</div>

							<div class="w-full grid grid-cols-1 md:grid-cols-2 gap-6">
								<div class="md:col-span-2">
									<label class="block text-sm font-semibold text-slate-700 mb-2">Company Name</label>
									<input type="text" name="nom_entreprise" value="{{ company.nom_entreprise }}" class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-indigo-500 outline-none transition">
								</div>

								<div>
									<label class="block text-sm font-semibold text-slate-700 mb-2">Location / Address</label>
									<input type="text" name="adresse_entreprise" value="{{ company.adresse_entreprise }}" class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-indigo-500 outline-none transition" placeholder="e.g. Casablanca, Morocco">
								</div>

								<div>
									<label class="block text-sm font-semibold text-slate-700 mb-2">Website</label>
									<input type="url" name="site_web" value="{{ company.site_web }}" class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-indigo-500 outline-none transition" placeholder="https://www.example.com">
								</div>
							</div>
						</div>

						<div class="px-8 py-4 bg-slate-50 border-t border-slate-100 flex justify-end">
							<button type="submit" class="bg-indigo-600 hover:bg-indigo-700 text-white px-6 py-2 rounded-lg font-medium transition flex items-center gap-2">
								<svg class="w-4 h-4" fill="none" stroke="currentColor" viewbox="0 0 24 24">
									<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
								</svg>
								Update Profile
							</button>
						</div>
					</form>
				</div>
			</div>
		</main>
	</body>
</html>
