<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>View Candidate | TalentHub</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        body { font-family: 'Inter', sans-serif; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 3px; }
        ::-webkit-scrollbar-thumb:hover { background: #94a3b8; }
    </style>
</head>

<body class="bg-slate-50 text-slate-600 antialiased h-screen flex overflow-hidden">

    {% include 'recruiter/sidebar.html.twig' %}

    <main class="flex-1 flex flex-col h-screen overflow-hidden bg-slate-50/50">
        
        <header class="h-20 bg-white border-b border-slate-100 flex items-center justify-between px-8 shadow-sm z-10">
            <div class="flex items-center gap-4">
                <a href="/recruiter/applications" class="text-slate-400 hover:text-slate-600 transition-colors">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path></svg>
                </a>
                <div>
                    <h1 class="text-xl font-bold text-slate-800 tracking-tight">Candidate Details</h1>
                    <p class="text-xs text-slate-400 mt-0.5">View application information</p>
                </div>
            </div>
            <div class="h-10 w-10 rounded-full bg-gradient-to-tr from-indigo-500 to-purple-500 border-2 border-white shadow-md flex items-center justify-center text-white font-bold text-sm">
                {{ session.user.name|slice(0,1)|upper }}
            </div>
        </header>

        <div class="flex-1 overflow-x-hidden overflow-y-auto p-8">

            {# Flash Messages #}
            {% if session.success %}
                <div class="mb-6 bg-emerald-50 border border-emerald-200 text-emerald-800 px-6 py-4 rounded-2xl flex items-center gap-3 shadow-sm">
                    <svg class="w-5 h-5 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                    <span class="font-medium">{{ session.success }}</span>
                </div>
            {% endif %}

            {% if session.error %}
                <div class="mb-6 bg-red-50 border border-red-200 text-red-800 px-6 py-4 rounded-2xl flex items-center gap-3 shadow-sm">
                    <svg class="w-5 h-5 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                    <span class="font-medium">{{ session.error }}</span>
                </div>
            {% endif %}

            <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                
                {# Candidate Profile Card #}
                <div class="lg:col-span-1">
                    <div class="bg-white rounded-3xl shadow-sm border border-slate-100 p-8">
                        <div class="text-center">
                            <div class="w-24 h-24 mx-auto rounded-full bg-gradient-to-br from-indigo-400 to-purple-400 flex items-center justify-center font-bold text-white text-3xl shadow-lg mb-4">
                                {{ application.candidate_nom|slice(0,1)|upper }}{{ application.candidate_prenom|slice(0,1)|upper }}
                            </div>
                            
                            <h2 class="text-2xl font-bold text-slate-800 mb-1">
                                {{ application.candidate_nom }} {{ application.candidate_prenom }}
                            </h2>
                            
                            <p class="text-sm text-slate-400 mb-4">{{ application.candidate_email }}</p>
                            
                            <div class="inline-flex items-center gap-1.5 px-4 py-2 rounded-full text-sm font-semibold border mb-6
                                {% if application.status == 'acceptee' %}bg-emerald-50 text-emerald-600 border-emerald-100
                                {% elseif application.status == 'refusee' %}bg-red-50 text-red-600 border-red-100
                                {% else %}bg-amber-50 text-amber-600 border-amber-100{% endif %}">
                                <span class="w-2 h-2 rounded-full 
                                    {% if application.status == 'acceptee' %}bg-emerald-500
                                    {% elseif application.status == 'refusee' %}bg-red-500
                                    {% else %}bg-amber-500{% endif %}">
                                </span>
                                {% if application.status == 'en_attente' %}Pending
                                {% elseif application.status == 'acceptee' %}Accepted
                                {% elseif application.status == 'refusee' %}Rejected
                                {% else %}{{ application.status|capitalize }}{% endif %}
                            </div>
                        </div>

                        <div class="border-t border-slate-100 pt-6 mt-6">
                            <h3 class="text-xs font-semibold text-slate-400 uppercase tracking-wider mb-4">Contact Information</h3>
                            
                            <div class="space-y-3">
                                <div class="flex items-center gap-3 text-sm">
                                    <svg class="w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path></svg>
                                    <span class="text-slate-600">{{ application.candidate_email }}</span>
                                </div>
                            </div>
                        </div>

                        {% if application.status == 'en_attente' %}
                        <div class="border-t border-slate-100 pt-6 mt-6">
                            <div class="flex gap-3">
                                <form method="POST" action="/recruiter/applications/accepter/{{ application.id }}" class="flex-1">
                                    <button type="submit" 
                                            onclick="return confirm('Accepter cette candidature ?');"
                                            class="w-full bg-emerald-500 hover:bg-emerald-600 text-white font-semibold py-3 px-4 rounded-xl transition-colors flex items-center justify-center gap-2">
                                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                                        Accepter
                                    </button>
                                </form>
                                
                                <form method="POST" action="/recruiter/applications/refuser/{{ application.id }}" class="flex-1">
                                    <button type="submit" 
                                            onclick="return confirm('Refuser cette candidature ?');"
                                            class="w-full bg-red-500 hover:bg-red-600 text-white font-semibold py-3 px-4 rounded-xl transition-colors flex items-center justify-center gap-2">
                                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                                        Refuser
                                    </button>
                                </form>
                            </div>
                        </div>
                        {% endif %}
                    </div>
                </div>

                {# Application Details #}
                <div class="lg:col-span-2">
                    <div class="bg-white rounded-3xl shadow-sm border border-slate-100 p-8">
                        <h3 class="text-lg font-bold text-slate-800 mb-6">Application Details</h3>
                        
                        <div class="space-y-6">
                            {# Job Information #}
                            <div>
                                <label class="text-xs font-semibold text-slate-400 uppercase tracking-wider block mb-2">Position Applied For</label>
                                <div class="flex items-center gap-3 p-4 bg-slate-50 rounded-xl border border-slate-100">
                                    <div class="p-3 bg-indigo-100 rounded-lg">
                                        <svg class="w-6 h-6 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path></svg>
                                    </div>
                                    <div>
                                        <div class="font-bold text-slate-800">{{ application.job_title }}</div>
                                        <div class="text-sm text-slate-500 flex items-center gap-1">
                                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path></svg>
                                            {{ application.company_name }}
                                        </div>
                                    </div>
                                </div>
                            </div>

                            {# Application Date #}
                            <div>
                                <label class="text-xs font-semibold text-slate-400 uppercase tracking-wider block mb-2">Application Date</label>
                                <div class="flex items-center gap-3 p-4 bg-slate-50 rounded-xl border border-slate-100">
                                    <div class="p-3 bg-amber-100 rounded-lg">
                                        <svg class="w-6 h-6 text-amber-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>
                                    </div>
                                    <div>
                                        <div class="font-semibold text-slate-800">{{ application.created_at|date('F d, Y') }}</div>
                                        <div class="text-sm text-slate-500">{{ application.created_at|date('g:i A') }}</div>
                                    </div>
                                </div>
                            </div>

                            {# Additional Information Section #}
                            <div>
                                <label class="text-xs font-semibold text-slate-400 uppercase tracking-wider block mb-2">Additional Information</label>
                                <div class="p-6 bg-slate-50 rounded-xl border border-slate-100">
                                    <p class="text-sm text-slate-600 leading-relaxed">
                                        Le candidat a soumis sa candidature pour le poste de <strong>{{ application.job_title }}</strong> 
                                        chez <strong>{{ application.company_name }}</strong>. 
                                        Vous pouvez examiner les qualifications du candidat et prendre une décision concernant sa candidature.
                                    </p>
                                </div>
                            </div>

                            {# Timeline #}
                            <div>
                                <label class="text-xs font-semibold text-slate-400 uppercase tracking-wider block mb-3">Application Timeline</label>
                                <div class="space-y-3">
                                    <div class="flex gap-4">
                                        <div class="flex flex-col items-center">
                                            <div class="w-3 h-3 rounded-full bg-indigo-500"></div>
                                            <div class="w-0.5 h-full bg-slate-200 mt-1"></div>
                                        </div>
                                        <div class="pb-6">
                                            <div class="font-semibold text-slate-800 text-sm">Application Submitted</div>
                                            <div class="text-xs text-slate-500 mt-1">{{ application.created_at|date('F d, Y - g:i A') }}</div>
                                        </div>
                                    </div>

                                    {% if application.status != 'en_attente' %}
                                    <div class="flex gap-4">
                                        <div class="flex flex-col items-center">
                                            <div class="w-3 h-3 rounded-full {% if application.status == 'acceptee' %}bg-emerald-500{% else %}bg-red-500{% endif %}"></div>
                                        </div>
                                        <div>
                                            <div class="font-semibold text-slate-800 text-sm">
                                                {% if application.status == 'acceptee' %}Application Accepted{% else %}Application Rejected{% endif %}
                                            </div>
                                            <div class="text-xs text-slate-500 mt-1">Decision made</div>
                                        </div>
                                    </div>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </main>
</body>
</html>