<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Create Account | TalentHub</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>

<body class="min-h-screen flex items-center justify-center bg-slate-50 p-6">

    <div class="w-full max-w-lg">
        
        <div class="flex justify-center mb-8">
            <div class="flex items-center gap-3">
                <div class="w-10 h-10 bg-indigo-600 rounded-xl flex items-center justify-center text-white font-bold text-xl shadow-lg shadow-indigo-200">
                    T
                </div>
                <span class="text-2xl font-bold text-slate-800 tracking-tight">TalentHub</span>
            </div>
        </div>

        <div class="bg-white rounded-3xl shadow-xl shadow-slate-200/50 border border-slate-100 overflow-hidden">
            <div class="p-8 md:p-10">
                
                <h1 class="text-2xl font-bold text-center text-slate-900 mb-2">Get started for free</h1>
                <p class="text-center text-slate-500 mb-8 text-sm">Create your account to start your journey.</p>

                {% if error %}
                    <div class="mb-6 rounded-xl border border-red-100 bg-red-50 px-4 py-3 text-sm text-red-600 flex items-center gap-2">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                        {{ error }}
                    </div>
                {% endif %}

                {% if errors %}
                    <div class="mb-6 rounded-xl border border-red-100 bg-red-50 px-4 py-3 text-sm text-red-600">
                        <p class="font-medium mb-1">Please correct the following errors:</p>
                        <ul class="list-disc pl-5 space-y-1 opacity-90">
                            {% for field, message in errors %}
                                <li>{{ message }}</li>
                            {% endfor %}
                        </ul>
                    </div>
                {% endif %}

                <form method="POST" action="/register" class="space-y-5">

                    <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
                        <div>
                            <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">
                                First Name
                            </label>
                            <input 
                                type="text" 
                                name="prenom" 
                                value="{{ old.prenom ?? '' }}"
                                required
                                placeholder="Jane"
                                class="w-full bg-slate-50 border border-slate-200 text-slate-900 text-sm rounded-xl focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 block w-full p-3 transition-all outline-none"
                            >
                        </div>
                        <div>
                            <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">
                                Last Name
                            </label>
                            <input 
                                type="text" 
                                name="nom" 
                                value="{{ old.nom ?? '' }}"
                                required
                                placeholder="Doe"
                                class="w-full bg-slate-50 border border-slate-200 text-slate-900 text-sm rounded-xl focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 block w-full p-3 transition-all outline-none"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">
                            Email Address
                        </label>
                        <input 
                            type="email" 
                            name="email" 
                            value="{{ old.email ?? '' }}"
                            required
                            placeholder="jane@example.com"
                            class="w-full bg-slate-50 border border-slate-200 text-slate-900 text-sm rounded-xl focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 block w-full p-3 transition-all outline-none"
                        >
                    </div>

                    <div>
                        <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-3">
                            I want to...
                        </label>
                        <div class="grid grid-cols-2 gap-4">
                            <label class="relative cursor-pointer group">
                                <input type="radio" name="role_id" value="3" class="peer sr-only" {% if (old.role_id ?? 3) == 3 %}checked{% endif %}>
                                <div class="p-4 rounded-xl border-2 border-slate-100 bg-white hover:border-indigo-200 transition-all peer-checked:border-indigo-600 peer-checked:bg-indigo-50 peer-checked:text-indigo-700">
                                    <div class="flex flex-col items-center text-center gap-2">
                                        <span class="text-2xl">👨‍💻</span>
                                        <span class="text-sm font-semibold">Find a Job</span>
                                    </div>
                                </div>
                            </label>

                            <label class="relative cursor-pointer group">
                                <input type="radio" name="role_id" value="2" class="peer sr-only" {% if (old.role_id ?? 3) == 2 %}checked{% endif %}>
                                <div class="p-4 rounded-xl border-2 border-slate-100 bg-white hover:border-indigo-200 transition-all peer-checked:border-indigo-600 peer-checked:bg-indigo-50 peer-checked:text-indigo-700">
                                    <div class="flex flex-col items-center text-center gap-2">
                                        <span class="text-2xl">💼</span>
                                        <span class="text-sm font-semibold">Hire Talent</span>
                                    </div>
                                </div>
                            </label>
                        </div>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
                        <div>
                            <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">
                                Password
                            </label>
                            <input 
                                type="password" 
                                name="password" 
                                required
                                placeholder="••••••••"
                                class="w-full bg-slate-50 border border-slate-200 text-slate-900 text-sm rounded-xl focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 block w-full p-3 transition-all outline-none"
                            >
                        </div>
                        <div>
                            <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">
                                Confirm
                            </label>
                            <input 
                                type="password" 
                                name="password_confirm" 
                                required
                                placeholder="••••••••"
                                class="w-full bg-slate-50 border border-slate-200 text-slate-900 text-sm rounded-xl focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 block w-full p-3 transition-all outline-none"
                            >
                        </div>
                    </div>

                    <div class="pt-2">
                        <button 
                            type="submit" 
                            class="w-full text-white bg-indigo-600 hover:bg-indigo-700 font-semibold rounded-xl text-sm px-5 py-3.5 text-center shadow-lg shadow-indigo-200 transition-all transform hover:scale-[1.01]"
                        >
                            Create Account
                        </button>
                    </div>

                </form>
            </div>

            <div class="bg-slate-50 px-8 py-6 border-t border-slate-100 text-center">
                <p class="text-sm text-slate-600">
                    Already have an account? 
                    <a href="/login" class="font-semibold text-indigo-600 hover:text-indigo-700 hover:underline">
                        Sign in
                    </a>
                </p>
            </div>
        </div>
        
        <div class="mt-8 text-center">
            <p class="text-xs text-slate-400">© 2024 TalentHub. Secure & Confidential.</p>
        </div>

    </div>

</body>
</html>